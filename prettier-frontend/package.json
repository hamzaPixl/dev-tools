{
  "name": "prettier-frontend-config",
  "private": true,
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "scripts": {
    "format": "prettier --check .",
    "format:fix": "prettier --write ."
  }
}
